//Дано трехзначное число. Проверить истинность высказывания: «Цифры данного числа образуют возрастающую или убывающую последовательность».
#include <math.h> // подключаемем библиотеку для использования математических операций
#include <stdio.h> // подключаем библиотеку ввод/вывода на экран
int main() //определяем функцию
{

	int  a,a1,a2,a3;// вводим переменные без значени 
	printf("Enter the a ");//просим пользователя ввести 
	scanf_s("%d", &a);//ввод
	a1 = a / 100;
	a2 = a / 10%10;
	a3 = a%10;

	if ((a1>a2) && (a2>a3))
		printf("ubivaet ");
	if ((a1<a2) && (a2 < a3))
	 printf("vosrastaet ");
	return 0;
}
