//Робот может перемещаться в четырех направлениях («С» — север, «З» — запад, «Ю» — юг, «В» — восток) и принимать три цифровые команды: 0 — продолжать движение, 1 — поворот налево, −1 — поворот направо. 
//Дан символ C — исходное направление робота и целое число N — посланная ему команда. Вывести направление робота после выполнения полученной команды
#include <math.h> // подключаемем библиотеку для использования математических операций
#include <stdio.h> // подключаем библиотеку ввод/вывода на экран
#include <stdlib.h>
#include <string.h>
int main() //определяем функцию
{
	system("chcp 1251");
	int  n;// вводим переменные без значени ;
	printf("Enter n  ");//просим пользователя ввести  
	scanf_s("%d", &n);//ввод
	
		switch (n)
		{
		case 1:
			printf("в");
			break;
		case 0:
			printf("c");
			break;
		case -1:
			printf("з");
			break;
		}

	return 0;
}
