//На плоскости расположены три точки: A, B, C.
//Определить, какая из двух последних точек (B или C) расположена ближе к A, и вывести эту точку и ее расстояние от точки A.
#include <math.h> // подключаемем библиотеку для использования математических операций
#include <stdio.h> // подключаем библиотеку ввод/вывода на экран
int main() //определяем функцию
{

	int  a,b,c,d,d1;// вводим переменные без значени 
	printf("Enter  a ");//просим пользователя ввести 
	scanf_s("%d", &a);//ввод
	printf("Enter  b ");//просим пользователя ввести 
	scanf_s("%d", &b);//ввод
	printf("Enter  c ");//просим пользователя ввести 
	scanf_s("%d", &c);//ввод
	if (a >= b)
		d = a - b;
	else d = b - a;
		if (a >= c)
			d1 = a - c;
		else d1 = c - a;
	if ((d) >= (d1)) 
		printf("c closer  %d ",d1);
	else printf("b closer %d ", d);
	return 0;
}
