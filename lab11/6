//Дано целое число, лежащее в диапазоне 1–999.
//Вывести его строку описание вида «четное двузначное число», «нечетное трехзначное число» и т. д.
#include <math.h> // подключаемем библиотеку для использования математических операций
#include <stdio.h> // подключаем библиотеку ввод/вывода на экран
#include <stdlib.h>
int main() //определяем функцию
{
	system("chcp 1251");
	int  a,i;// вводим переменные без значени 
	printf("Enter  a ");//просим пользователя ввести 
	scanf_s("%d", &a);//ввод
	i = 0;
	if (a%2==0)
		printf("четное  ");
	else printf("нечетное  "); 
	while (a != 0) {
		i++;
		a /= 10;
	}
		if (i == 3) printf("трехзначное  ");
		if (i == 2) printf("двузначное  ");
		if (i == 1) printf("однозначное  ");
	return 0;
}
